import { DataCharacters } from '../modules/characters/classes/data-characters';

describe('DataCharacters', () => {
  it('should create an instance', () => {
    expect(new DataCharacters()).toBeTruthy();
  });
});
